#include <stdio.h>
#include <stdlib.h>

/**
*main-removesqande
*
*Return: Always 0.
*/
int main(void)
{
	char x;

	for (x = 'a'; x <= 'z'; x++)
		putchar("x && !q || !e");
			putchar('\n');
	return (0);
}
