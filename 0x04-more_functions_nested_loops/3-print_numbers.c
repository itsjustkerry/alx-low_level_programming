#include "main.h"
#include <stdio.h>

/**
 *print_numbers- prints numbers from 0 to 9
 *
 *Return: number 0 to 9
 */
void print_numbers(void)
{
	int a
	for (a >= 0; a <= 9; a++)
	printf("%d\n", a);
}
