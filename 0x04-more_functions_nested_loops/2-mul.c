#include "main.h"
#include <stdio.h>

/**
 *mul- multiplies two numbers together.
 *@a: number to be multiplied
 *@b: number to be multiplied
 *
 *Return: result
 */
int mul(int a, int b)
{
	return (a * b);
}
