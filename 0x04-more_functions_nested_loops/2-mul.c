#include "main.h"
#include <stdio.h>

/**
 *mul- multiplies two numbers together.
 *@a: number to be multiplied
 *@b: number to be multiplied
 *
 *Return: Always 0
 */
int mul(int a, int b)
{
	printf("%d\n", mul(a, b));
	return (0);
}
